apiVersion: apps/v1
kind: Deployment
metadata:
  name: comment-service
  labels:
    app: comment-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: comment-service
  template:
    metadata:
      labels:
        app: comment-service
    spec:
      containers:
        - name: comment-service
          image: ghcr.io/hoangdinhbui/link-sphere/comment-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8004
          env:
            - name: SERVER_PORT
              value: "8004"
            - name: POSTGRES_HOST
              value: postgres
            - name: POSTGRES_PORT
              value: "5432"
            - name: POSTGRES_USER
              value: linksphere
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: linksphere-secrets
                  key: postgres-password
            - name: POSTGRES_DB
              value: linksphere
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: linksphere-secrets
                  key: jwt-secret
            - name: KAFKA_BROKERS
              value: kafka-svc:9092
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: comment-service
spec:
  selector:
    app: comment-service
  ports:
    - port: 8004
      targetPort: 8004
  type: ClusterIP
